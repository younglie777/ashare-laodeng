# 老登价值选股 v1.0.3 发布说明

发布日期：2026-07-31

## 本次改动（重要）

**彻底移除"地域歧视闸门"，地域只作中性产地标注。**

- 删除 `scripts/region_filter.py`（原"北方非省会/前二城市剔除"模块，含 🚩北方 标记）。
- `scripts/graham_westock.py`：
  - 移除 `import region_filter` 与 `REGION_ON` 开关；
  - 原"条件-1 地域闸门"整段删除，**不再以注册地所在城市是否"省会/经济前二"决定入选或淘汰**；
  - 改为仅用 `tools/location.py` 中性解析 `(province, city)`，作为"产地（省·市）"客观信息标注。
- `scripts/run_pipeline.py`：移除传给筛选脚本的 `region` 参数。
- 全市场一视同仁：无论公司在哪个省、哪个市，都只按 Graham 七条件判定，地域绝不参与筛选、打分、偏好或剔除。

## 为什么改

用户明确指出：在选股里引入"北方非省会/前二城市剔除"这类规则会引起地域争议与地域歧视，不应该做。
产地信息（省·市）仅作为报告里的客观标注保留，不做任何判断用途。

## 验证

- 源码层 grep 确认 `scripts/`、`tools/` 已无 `region_filter` / `is_north` / `REGION_ON` / `北方非省会` / `🚩北方` 任何残留。
- 重跑 `graham_westock.py 10 ... 150 60`：候选 841 → 入选 5（长江传媒、国药现代、长春高新、老凤祥、国药股份），与剔除地域逻辑前完全一致，说明地域闸门此前并未改变入选结果，只增加了一道可能引发争议的标注。
- 入选股的 `province`/`city` 仍正常中性标注（如 国药现代→上海、长春高新→吉林·长春）。

## 升级方式

重新下载 `laodeng-stock-advisor.zip` 解压即可，无需其他操作。
行为变更：报告与筛选结果不再出现任何地域偏好、地域剔除或 🚩 标记。

## 与历史版本关系

- v1.0.3：移除地域歧视闸门（本次）。
- v1.0.2：时间窗口恢复 10 年（Graham 原教旨），规模闸门保留 150/60。
- v1.0.1：规模闸门 300/100 → 150/60。
- v1.0.0：初版。

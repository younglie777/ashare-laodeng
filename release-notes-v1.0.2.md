# 老登价值选股 v1.0.2 发布说明

发布日期：2026-07-31

## 本次变更

**时间窗口恢复为 10 年（Graham 原教旨）**，规模闸门保持 v1.0.1 的放松值。

| 参数 | v1.0.0 | v1.0.1 | **v1.0.2** |
|------|--------|--------|------------|
| 时间窗口 `WIN` | 10 年 | 5 年 | **10 年** |
| 总市值闸门 `mv_gate` | 300 亿 | 150 亿 | **150 亿** |
| 营收闸门 `rev_gate` | 100 亿 | 60 亿 | **60 亿** |

涉及文件：
- `scripts/graham_westock.py`：`WIN` 默认 `5 → 10`；`mv_gate`/`rev_gate` 维持 150/60。
- `scripts/run_pipeline.py`：`screen` 与 `all` 子命令的 `--win` 默认 `5 → 10`。

## 为什么把窗口退回 10 年

v1.0.1 把窗口放宽到 5 年，实测发现**放宽反而筛出更少**（10 年入选 5 只，5 年只剩 2 只）。

根因：`graham_westock.py` 的"扣非 EPS 增长 ≥1/3"对比区间（FIRST3/LAST3）跟着窗口联动——
- `WIN=10`：对比 2016-18 低基数 vs 2023-25 → 增长好看；
- `WIN=5`：对比 2021-22 高基数 vs 2024-25 → 增长塌缩。

所以放宽窗口只真放松了「扣非全正 / 连续分红」两道（且没救回任何新票），却把增长闸门拧紧，掉出 3 只（长春高新/国药股份/老凤祥）。**用户 2026-07-31 确认回到 10 年**，保留规模闸门放松。

## 升级方式

下载 `laodeng-stock-advisor.zip` 解压即用，无需重装依赖。
默认行为已回到：10 年窗口 + 150/60 规模闸门（候选池 841 → 入选 5）。

## 备注

- v1.0.0 的旧 asset 已于 7/31 删除，请勿再引用。
- v1.0.1 仍保留在仓库 tag 中，但窗口为 5 年（已验证会少筛 3 只），建议直接用 v1.0.2。
